# Story 4.3 — env for serving dispatch-send LOCALLY against the provider mock (tests/provider-mock.ts).
# No secrets here: the mock accepts any key, and this cron secret only guards the locally served function.
#   supabase functions serve dispatch-send --env-file supabase/mock.env --no-verify-jwt
# The Edge Runtime container reaches the mock on the host through host.docker.internal (Linux CI: --add-host).
# The real provider's PROVIDER_BASE_URL / PROVIDER_API_KEY and the real CRON_SECRET live in the git-ignored
# supabase/.env and in the hosted function secrets (`supabase secrets set`) — never in this file.
PROVIDER_BASE_URL=http://host.docker.internal:8787
PROVIDER_API_KEY=mock
CRON_SECRET=local-cron-secret
# Story 6.3 (probe row a): the dispatch-sweep cron job is enabled by 0013_cron_poll.sql and, in the same step, retries
# are switched on here and in the hosted secrets — a provider 5xx / timeout now leaves the send `dispatched` under its
# lease for the sweep to replay the same Idempotency-Key (cap 3 attempts); unset it to get D-7's immediate `partial`.
DISPATCH_RETRY_ENABLED=on
# SUPABASE_DB_URL (poll-events' direct Postgres connection) is injected by `supabase functions serve` itself
# (postgresql://postgres:postgres@supabase_db_vg-campaign-portal:5432/postgres) and by the platform hosted; names
# starting with SUPABASE_ are refused in env files and in `supabase secrets set`, so it is never written here.
