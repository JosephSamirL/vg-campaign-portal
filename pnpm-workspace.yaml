# pnpm asks before running install scripts; these two are safe build steps.
# pnpm 10/11 read `onlyBuiltDependencies`; pnpm 12 reads `allowBuilds` — keep both.
onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
allowBuilds:
  esbuild: true
  unrs-resolver: true
